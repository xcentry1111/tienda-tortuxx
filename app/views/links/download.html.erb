<div class="row">
<div class="col-md-10 center-block no-float"
	<div class="article card large-padding text-center">
		<h1> Links descarga</h1>
		<p> 
			Este link expirará en 
			<%= distance_of_time_in_words(DateTime.now,@link.expiration_date) %>
		</p>
		<p> 
			Podrás acceder a él
			<%= 5 - @link.downloads %>
			veces más
		<div >
			<% @link_attachments.each do |link_attachment| %>
					<%= link_to link_attachment.attachment.archivo_file_name, download_attachment_path(id: @link.custom_id,attachment_id:link_attachment.id)%>
			<% end -%>
			</div>
</div>
</div>
					